# 内部質問システム全面監査報告書（2026-08-03）

## 結論

UIの見た目を除き、質問選択、候補評価、回答反映、候補除外、推測移行、質問上限、追加質問、回答修正、戻る操作、途中保存・復帰を監査しました。2件の内部不具合を修正し、全回帰テストに合格しました。

## 修正した不具合

### 1. 情報量0の質問を出題するフォールバック

`entropyPick()` は通常、現在の候補を「はい」と「いいえ」に分割できない質問を除外します。しかし、有効な質問が1件も残らなかった場合だけ `unused[0]` を返しており、全候補の答えが同じで一件も絞れない質問を出す可能性がありました。

修正後は `null` を返し、`renderQuestion()` の既存終了判定を通して推測へ移ります。質問上限まで無意味な質問を繰り返す経路を閉じました。

### 2. 回答イベントの二重処理

回答ボタンの連打、古い画面から遅れて届くイベント、外部からの重複呼び出しに対する内部ガードがありませんでした。同じ質問へ短時間に2回答が届くと、回答ログや候補スコアが二重更新される可能性がありました。

修正後は次の両方を満たす回答だけを受理します。

1. 回答キーが現在表示中の未回答質問と一致する。
2. `history` が `answerLog` よりちょうど1件多い。

一度処理済みの質問、古い質問、未表示の質問への回答は状態を変更せず終了します。

## 監査対象

- 質問キーのモード別有効化
- 情報量0質問の排除
- 二手先ミニマックスによる質問選択
- 初手ランダム化
- 主観・客観質問の加減点差
- 「たぶん」の半加重
- 「わからない」の無加点と最大10問救済
- 通常34問、追加5問、絶対50問の上限
- 8地方質問の最大1回・3問後・最大勢力25～75%制限
- 都道府県質問の解禁時期・間隔・回数制限
- 同一カテゴリの連続抑制
- 人口質問の上下限推論と重複排除
- 客観回答による即時候補除外
- 主観回答で候補を即時除外しない保護
- 誤推測後の除外候補復帰
- 回答修正時の全回答再生
- 戻る操作時の質問・回答ログ整合性
- 途中保存値の型、版、期限、モード、質問、自治体検証
- Web版・Android版の同一ロジック同期

## 新設した再発防止テスト

`tools/test-question-engine.js` を追加し、通常の `npm test` に組み込みました。

- 情報量0質問のフォールバックが `null` であること。
- `unused[0]` に戻っていないこと。
- 現在の未回答質問だけを受理する二重回答ガードがあること。
- 「わからない」救済上限10問を維持すること。
- 絶対質問上限50問を維持すること。
- 追加質問を5問に固定すること。
- 8地方質問の総当たり防止条件を維持すること。

## 検証結果

- 自治体データ：1,742レコード、エラー0、警告0。
- 質問タグ：使用中1,143種類、整合性検査合格。
- 内部質問エンジン回帰テスト：合格。
- ゲームセッション保存・復帰：合格。
- 戻る操作・回答ログ整合性：合格。
- Web/Nativeビルド、Androidアセット同期：合格。
- 全 `npm test`：合格。
