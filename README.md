# おらマチ

**おらマチ**は、Yes / No の質問に答えていくと、あなたのマチを当てる市町村当てゲームです。

日本の地理、交通、文化、方言、名物グルメ、ご当地キャラなどを手がかりに、だんだん候補を絞り込んでいきます。

## 公開ページ

https://oramachi-jp.com/

## 遊び方

1. 表示される質問に「はい」「いいえ」で答えます。
2. わからない質問はスキップできます。
3. 候補が絞られると、おらっちがマチを推測します。
4. 当たったら診断カード風の結果画面が表示され、「あなたの地元がバレた理由(地元バレポイント)」が表示されます。
5. 結果は画像または文章でシェアできます。
6. 正解した自治体は「全国制覇帳」に記録されます。

## 現在の主な機能

* Yes / No による市町村当て
* 候補数の表示
* スキップ機能
* 診断カード風の結果画面
* 地元バレポイント(決め手になった回答を最大3つ表示)
* 画像でのシェア(端末内で作成、外部送信なし)・Xでの文章シェア
* 全国制覇帳(正解した自治体・都道府県別/地方別の達成状況・最少質問数などを端末内に記録)
* 匿名のプレイ結果送信のON/OFF設定
* 名物グルメ、方言、ご当地キャラ、豆知識の表示
* 新潟県版モード（ワタシの地元なので）
* 全国版と都道府県版・地方版のモード選択
* 人口・面積・人口密度を使った統計質問

## 使用技術

* HTML
* CSS
* JavaScript
* JSON
* GitHub Pages
* Canvas API(結果画像の生成。外部ライブラリ・外部CDNは使用していません)
* localStorage(全国制覇帳・設定の保存。外部送信はしていません)

## データ出典

### 人口・面積(統計質問で利用)

各自治体の `stats.population`(人口)・`stats.area_km2`(面積)は、以下を基準にしています。

* 基準年月日: **2025年10月1日**(全自治体で統一)
* 出典: [uub.jp「全国の市区町村 人口・面積・人口密度ランキング」](https://uub.jp/rnk/cktv_j.html)
  * 人口の元データ: 総務省統計局 令和7年国勢調査人口速報値に基づく推計人口
  * 面積の元データ: 国土交通省 国土地理院「全国都道府県市区町村別面積調」(2025年10月1日時点)
* 「東京23区部」(23区全体の集計エントリ)の人口・面積は、23区それぞれの値を合算したもの。
* 通常自治体を対象とした面積順位・人口密度順位・上位/下位20%の分位計算には、「東京23区部」を含めていません。
* stats を設定できなかった自治体はありません(全826自治体に適用済み)。

### 全国制覇帳・設定(localStorage)

* 正解した自治体の記録(`oramachi_conquest_v1`)と、匿名送信のON/OFF設定(`oramachi_settings_v1`)は、
  お使いの端末のブラウザ内(`localStorage`)だけに保存され、外部へは送信されません。
* 自治体の一意キーには、cities.jsonに専用のID列が無いため「都道府県+名前」の組み合わせ(`cityId()`関数)を使っています。
* データが壊れている・バージョンが古い場合は、自動的に初期状態から作り直します(エラーで落ちることはありません)。
* 詳しい送信内容は [privacy.html](./privacy.html) を参照してください。

## 検証用スクリプト(開発者向け・ゲーム本体からは読み込まれません)

いずれも Node.js で実行します。`harness.js` が `app.js` を実際の実行時と同じ形で読み込むため、
シミュレーション・判別困難ペア検出のロジックは、ゲーム本体の質問選択・候補更新ロジックをそのまま利用しています。

### 自動シミュレーション (`simulate.js`)

全自治体を対象に、cities.json通りに正直に回答した場合の自動プレイを行い、正解率・平均質問数などを集計します。

```
node simulate.js                              # 全国版・seed 1,2,3で実行(既定)
node simulate.js --mode=kyushu --seeds=1      # 九州・沖縄版のみ
node simulate.js --mode=ALL_MODES --seeds=1   # 全国版+全地方版+新潟県版+東京都版をまとめて実行
node simulate.js --noise=1                    # 1問だけわざと誤答させる耐性テストつきで実行
```

結果は標準出力への表示に加え、`/mnt/user-data/outputs/simulation_report.json` に詳細を保存します。

### 判別困難ペア検出 (`find_twin_pairs.js` + `find_twin_pairs.py`)

全自治体のペアを比較し、ゲームで実際に使われる回答(タグ・人口/面積/人口密度の分類結果)がほぼ同じ
組み合わせを検出します。

```
node find_twin_pairs.js > tag_snapshot.json
python3 find_twin_pairs.py tag_snapshot.json
```

`/mnt/user-data/outputs/twin_pairs_report.md` と `.json` の両方に出力されます。

### デバッグモード

ゲームのURLに `?debug=1` を付けて開くと、画面下部に候補数・スコア・質問選択の評価内訳などを表示する
デバッグパネルが出ます(通常の公開画面には表示されません)。

### 全国制覇帳の整合性・性能検証 (`verify_conquest.js`)

`correct(true)`を多数回自動実行し、「画面に表示された自治体」と「全国制覇帳に実際に記録される自治体」が
必ず一致するか、大量プレイ時にlocalStorageの読み書きが性能問題を起こさないかを検証します。

```
node verify_conquest.js
```

この検証によって、一意マッチ経由の推測画面(`forcedGuessCity`)で「当たり!」を押した場合に、
ごく稀に確定される自治体が画面表示とずれることがある不具合を発見し、`correct()`が必ず
画面表示に使われた`lastGuessCity`を優先して参照するよう修正しました(300件連続プレイで
画面表示と制覇帳記録が100%一致することを確認済み。制覇帳への書き込み自体は1回あたり
0.4ms未満で、大量プレイでも性能上の問題はありません)。



## 今後追加したい機能

* 市区町村データのさらなる拡充(町村への対応拡大など)
* 質問項目の追加
* 全国制覇帳の地図表示
* 自治体の一意ID(現在は「都道府県+名前」を仮のIDとして使用)

## 作者コメント

地理や方言、名物グルメ、地元あるあるが好きなので、「アキネイターの市町村版」のようなゲームを作ってみました。

まだ対応自治体や質問数は少ないですが、少しずつデータを増やして日本全国のマチを当てられるゲームに育てていきたいです。


## 独自ドメイン・広告設定

* 公開ドメインは `https://oramachi-jp.com/` を想定しています。リポジトリ直下の `CNAME` は削除しないでください。
* DNS設定とAdSense申請の手順は `MONETIZATION_SETUP.md` を参照してください。
* AdSenseの `ca-pub-...`、広告枠ID、`ads.txt` のPublisher IDは、管理画面で本人に発行された値だけを使用してください。
* アフィリエイトリンクには、リンクの近くに見やすい「PR」または「広告」表示を付けてください。
