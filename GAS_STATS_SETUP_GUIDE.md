# おらマチ 「今日の挑戦者数」「みんなの統計」サーバー側の設定ガイド

## 前提: このファイルが必要になった背景

「おらマチ」はGitHub Pages等の**静的サイト**として作られており、専用のデータベース(Firestore等)は使っていません。
既存の「最近当てられたマチ」「プレイ回数」表示は、`app.js` 内の `CORRECTIONS_ENDPOINT_URL` が指す
**Google Apps Script(GAS)のWebアプリ**へゲーム結果をPOSTし、そのGASがGoogleスプレッドシートへ書き込む、
という既存の仕組みを使っています。

今回追加した以下の2機能も、**同じGASの仕組みを拡張する**ことで実現する設計にしています。

- トップページの「今日の挑戦者数」
- 「みんなの統計」ページ(苦戦した自治体ランキングなど)

**クライアント側(`app.js`)の実装は完了していますが、サーバー側(GASスクリプトの中身)は
このプロジェクトのファイルとしては存在せず、Google側で個別に管理されているため、
今回の作業では直接編集できていません。** 以下の対応をGAS側に追加してください。

---

## 1. クライアント側が送信するようになったデータ

### 1-1. 今日の挑戦者数用(匿名の来訪ログ)

トップページを開くたびに、**その日まだ送っていなければ1回だけ**、次の形式のデータがPOSTされます。

```json
{
  "type": "visitor",
  "visitorId": "ランダムな文字列(端末ごとに1つ、個人を特定できる情報ではない)",
  "dateJst": "2026-07-16",
  "timestamp": "2026-07-16T00:12:34.000Z"
}
```

- `visitorId` は `localStorage` に保存された使い捨てのランダム文字列です。氏名・メールアドレス・IPアドレス・広告IDなど、個人を特定できる情報は一切含まれません。
- 同じ端末・同じ日(日本時間)であれば、ページを何度再読み込みしても、この送信は最大1回しか行われません(クライアント側で送信済みかどうかを記録しているため)。

### 1-2. 既存のプレイ結果ログ(変更なし)

```json
{
  "type": "result",
  "result": "success" | "giveup",
  "cityName": "...", "pref": "...",
  "questionCount": 12,
  "timestamp": "..."
}
```

これは以前からある送信で、変更していません。「みんなの統計」の集計は、**主にこの `result` ログの蓄積**を使う想定です。

---

## 2. GAS側で対応してほしいこと

### 2-1. 今日の挑戦者数(`todayVisitors`)

1. POSTされた `type: "visitor"` のデータを、`dateJst` ごとにシートへ記録する(例: `visitorId` と `dateJst` の組を1行として追記)。
2. GETリクエストが来たとき、**今日の日付(JST)に該当する `visitorId` のユニーク件数**を数え、レスポンスJSONに `todayVisitors` という数値フィールドとして含める。

```json
{
  "totalPlays": 1234,
  "todayPlays": 56,
  "todayVisitors": 42,
  "recentSuccesses": ["十日町市", "..."]
}
```

`todayVisitors` が無い(未対応の)場合、トップページ側は数字を0と決めつけたり水増ししたりせず、
**この表示自体を出さない**ようになっています(そのままで安全です)。

### 2-2. みんなの統計(`statsDetail`)

GETレスポンスに、次の形の `statsDetail` オブジェクトを追加してください。
`statsDetail` が無い場合、統計ページ側は「まだ集計できるだけのデータがありません」という
案内を表示するだけで、存在しない数値をでっち上げて表示することはありません。

```json
{
  "statsDetail": {
    "minSampleSize": 10,
    "overallAccuracy": 87.3,
    "overallAvgQuestions": 17.8,
    "hardestCities": [
      { "name": "山県市", "pref": "岐阜県", "accuracy": 63.2, "avgQuestions": 24.1, "plays": 15 }
    ],
    "easiestCities": [
      { "name": "松本市", "pref": "長野県", "accuracy": 98.5, "avgQuestions": 12.3, "plays": 40 }
    ],
    "mostQuestionsCities": [
      { "name": "...", "pref": "...", "avgQuestions": 28.4, "plays": 12 }
    ],
    "fewestQuestionsCities": [
      { "name": "...", "pref": "...", "avgQuestions": 9.1, "plays": 20 }
    ]
  }
}
```

集計時の注意点(依頼要件どおりに実装してください):

- **`minSampleSize`(例: 10)未満のプレイ数しかない自治体は、ランキングから除外**してください。極端に少ないサンプルで「正答率100%」「0%」になるのを防ぐためです。
- `accuracy` は「正解ゲーム数 ÷ (正解+諦め)ゲーム数 × 100」。
- `avgQuestions` は、**正解したゲームの質問数だけの平均**を基本にしてください(諦めたゲームの質問数を混ぜない)。
- テストプレイや管理者の試行を除外できるよう、GAS側で除外用の判定(例: 特定の `visitorId` をブロックリストに入れる等)を用意することを推奨します。
- 同一端末からの短時間の大量プレイ(botや連打)を検知したら、集計対象から除外することを推奨します。

### 2-3. 期間別集計(任意)

「みんなの統計」ページには「累計 / 今月 / 直近30日」を切り替えるタブがすでに用意されています。
GAS側が複数期間分の `statsDetail` を返せるようになれば自動的に活用されますが、
**まずは「累計」のみの対応でも問題なく動作します**(タブ自体は表示されますが、その場合は
どのタブを押しても同じ「累計」のデータが表示されます)。将来的な拡張として、
`?period=month` や `?period=recent30` のようなクエリパラメータに応じて
集計期間を変えたJSONを返す形にすると、タブが実際に機能するようになります。

---

## 3. 費用・運用について

- Google Apps Scriptの実行自体は無料枠内で収まることがほとんどです(1日あたりの実行回数・実行時間に上限がありますが、個人サイト程度のアクセス数であれば通常問題になりません)。
- Googleスプレッドシートの行数が増え続けると、まれにGASの実行が遅くなることがあります。半年〜1年ごとに古いログを別シートへ退避する、などの運用を推奨します。
- 追加の有料サービスは必要ありません(Firestore等の新規契約は不要です)。

---

## 4. 対応が完了するとどうなるか

- GAS側が `todayVisitors` を返すようになった時点で、トップページに「今日の挑戦者 ◯◯人」が自動的に表示されます。クライアント側の追加対応は不要です。
- GAS側が `statsDetail` を返すようになった時点で、「みんなの統計」ページのランキングが自動的に埋まります。こちらもクライアント側の追加対応は不要です。
